# Secrets referenced as ${VAR} from mcp-servers.json.
# Copy to ~/.config/cc-container/mcp.env and chmod 600.
# Values reach the guest as --env, so they stay in the container's process
# environment and never land in its filesystem or the cc-home volume.
CONTEXT7_API_KEY=your-key-here
